// Copy this file to secrets.h and fill in your real values.
// secrets.h is gitignored — don't commit your credentials.

#pragma once

// WiFi
#define WIFI_SSID   "your-wifi-ssid"
#define WIFI_PASS   "your-wifi-password"

// mDNS / OTA hostname → http://<this>.local/
#define MDNS_NAME       "promass"
#define OTA_HOSTNAME    "promass"
#define OTA_PASSWORD    "change-me"

// Remote telemetry ingest endpoint (any HTTPS server that accepts POST + API key)
#define TELEMETRY_URL      "https://example.com/api/promass/"
#define TELEMETRY_API_KEY  "your-api-key-here"
#define DEVICE_ID        "PROMASS-1"
